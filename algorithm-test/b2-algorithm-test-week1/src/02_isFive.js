/*
    isFive

    숫자를 입력받아 5와 같으면 true를,
    그렇지 않으면 false를 리턴해야 합니다
*/

function isFive(num) {
  if (num === 5) {
    return true; // 여기서 작업하세요.
  } else {
    return false;
  }
}

module.exports = isFive;
